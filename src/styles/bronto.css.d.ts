/**
 * Bronto CSS bridge API stability.
 *
 * @public Stable for `0.1.x`: the `@ponchia/annotations/bronto.css` subpath,
 * package-prefixed annotation classes, and legacy `ui-annotation*`
 * compatibility styling.
 */
declare const stylesheet: string;
export default stylesheet;
